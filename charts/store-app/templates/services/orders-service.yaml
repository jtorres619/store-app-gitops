apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.orders.name }}
spec:
  selector:
    app: {{ .Values.services.orders.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.services.orders.port }}
      targetPort: {{ .Values.services.orders.port }}
  type: ClusterIP