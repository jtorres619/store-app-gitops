apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.users.name }}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: {{ .Values.services.users.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.users.name }}
    spec:
      containers:
        - name: {{ .Values.services.users.name }}
          image: "{{ .Values.services.users.image.repository }}:{{ .Values.services.users.image.tag }}"
          ports:
            - containerPort: {{ .Values.services.users.port }}
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
